"""Пакет `booklib` — CLI-приложение для ведения домашней библиотеки.

В пакете реализованы:
  * модели данных (:mod:`booklib.models`),
  * файловое хранилище JSON и CSV-импорт/экспорт (:mod:`booklib.storage`),
  * фильтры поиска и сортировки (:mod:`booklib.filters`),
  * обработчики команд CLI (:mod:`booklib.commands`).

Пакет рассчитан на использование из командной строки через модуль :mod:`main`.
Документация сгенерирована Sphinx и доступна в каталоге ``docs``.
"""

__all__ = ['models','storage','filters','commands']
